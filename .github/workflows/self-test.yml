name: self-test

on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  detect:
    runs-on: windows-2025
    strategy:
      fail-fast: false
      matrix:
        arch:
          - amd64
          - i386
    steps:
      - name: setup wdk7
        id: wdk7
        uses: tinysec/wdk7@master
        with:
          arch: ${{ matrix.arch }}
          download: "false"
          cache: "false"

      - name: show outputs
        shell: powershell
        run: |
          "found=${{ steps.wdk7.outputs.found }}"
          "root=${{ steps.wdk7.outputs.root }}"
          "arch=${{ steps.wdk7.outputs.arch }}"
          "source=${{ steps.wdk7.outputs.source }}"
          "cache-hit=${{ steps.wdk7.outputs.cache-hit }}"
          "toolchain-file=${{ steps.wdk7.outputs.toolchain-file }}"
